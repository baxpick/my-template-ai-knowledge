<!--
  Firebase Studio reads .idx/airules.md with HIGHER precedence than AGENTS.md, so
  without this pointer it could ignore AGENTS.md. This is a SHALLOW POINTER, not a
  copy of the rules: it redirects to AGENTS.md, the single source of agent
  instructions. Delete it if you never use Firebase Studio.
  See docs/reference/agent-adapters.md.
-->
Follow the instructions in AGENTS.md at the repository root. It is the single
source of truth for how to work in this repo. Read it before doing anything.
