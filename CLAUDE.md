@AGENTS.md

<!--
  Claude Code reads CLAUDE.md, not AGENTS.md. The `@AGENTS.md` line above is
  the officially documented import syntax: Claude inlines AGENTS.md at session
  start, so AGENTS.md stays the single source of agent instructions. Do NOT
  copy rules into this file. If truly Claude-specific instructions are ever
  needed, add them BELOW the import line. If the target repo already had a
  CLAUDE.md, merge its unique content into AGENTS.md / docs/ first, then keep
  only this import here (see docs/ai-doc-solution-maker.md).
-->
