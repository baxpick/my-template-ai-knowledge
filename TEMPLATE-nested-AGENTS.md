# <Area> — Agent Rules

<!--
  Copy this file to `AGENTS.md` inside any subdirectory that has its own rules.
  The closest AGENTS.md to the file being edited wins, so an agent working here
  gets these rules automatically without the root AGENTS.md carrying them.
  Keep it short (≤60 lines) and imperative — the RULES only, not prose. Point to
  docs/ for detail and to a runbook for the step-by-step procedure. Delete these
  comments and this template file itself once you have real nested files.
-->

Applies to everything under this directory.

## Always
1. ⟨rule⟩
2. ⟨rule⟩

## Never
- ⟨anti-rule⟩

## For detail / procedure
- Full reference: `⟨path to docs/… ⟩`
- Step-by-step: `⟨path to docs/runbooks/…⟩`
